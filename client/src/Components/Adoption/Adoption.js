import styled from "styled-components";
import react from "react";

const Adoption = () => {
	return <AdoptionWrapper>Adoption</AdoptionWrapper>;
};
const AdoptionWrapper = styled.div``;
export default Adoption;
